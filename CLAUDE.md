Stay on the `main` branch unless told otherwise (when automatically creating worktrees, merge them back in the end). Always commit changes. Do NOT push.

This is application code, not a library. Do not maintian backward compatibility.

Regularly simplify code to keep it clean and readable. Clean up stale code and dependencies. Remove unused imports, functions, classes, commented-out code, etc. Keep README and `pyproject.toml` updated.

Make sure that a webserver for showing the results always runs on `localhost:8050` and shows the most recent state.

Never cite unpublished/in-preparation work (e.g. from context material) in the README or Help page — published DOIs and preprints only.
